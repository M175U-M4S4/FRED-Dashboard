You are Codex. Build FRED Dashboard V2 as specified in SPEC_V2.md, using DATASETS_V2.yaml as the single source of truth for indicators and composites.

Hard requirements
- Catalog-driven: No hardcoded series in code (except tests).
- Robust ingestion: do not remove series because of temporary errors; implement retry + fallback_ids.
- Store raw and derived separately. Provide FastAPI endpoints and a UI (Streamlit is acceptable as default).
- Dark-mode minimal UI. Include domain tabs and composite pages.
- Provide a Makefile with:
  - make setup
  - make ingest
  - make run-api
  - make run-ui
  - make test

Implementation preferences
- Python 3.11+
- DuckDB preferred; SQLite acceptable with equivalent schema
- Use httpx or requests; use tenacity for retries; use aiolimiter or custom token bucket for rate limiting.
- Use pydantic for config models parsed from YAML.
- Ensure transformations:
  - yoy = (value / value_12m_ago - 1)*100 for monthly, or same-period-last-year for weekly/daily (use 365d nearest) with clear rule
  - mom for monthly
  - diff for levels
  - zscore with rolling window (default 5 years)

Files you must produce
- Full repo with code
- README with run instructions
- config/.env.example
- config/DATASETS_V2.yaml (use provided)
- SQL schema file
